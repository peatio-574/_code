# 复制为 .env 使用（deploy.sh 会按需自动生成）
# 说明：
#   - 若已把数据库连接写进 app/config.py，则 DATABASE_URL 可留空/删除，应用会用 config.py 内置值。
#   - 若密码含特殊字符（@ & / : 等），必须做 URL 编码：
#       @ -> %40   & -> %26   / -> %2F   : -> %3A   # -> %23   ? -> %3F
#     例如密码  BBii@BDIKCAU&QABiiBBi*JBTIH  应写成  BBii%40BDIKCAU%26QABiiBBi*JBTIH
SECRET_KEY=请改为一段随机长字符串
# DATABASE_URL=mysql+pymysql://用户名:URL编码后的密码@127.0.0.1:3306/job?charset=utf8mb4
